docker run --rm \
  -v "$(pwd)":/config \
  -v "$(pwd)/../components":/components \
  -it esphome/esphome \
  clean \
  hlink.yml \
  hlink_switch.yml \
  hlink_all.yml \
  hlink_all_arduino.yml \
  hlink_actions.yml