docker run --rm \
  -v "$(pwd)":/config \
  -v "$(pwd)/../components":/components \
  -it esphome/esphome \
  compile \
  hlink.yml \
  hlink_switch.yml \
  hlink_sensor.yml \
  hlink_sensor_arduino.yml \
  hlink_actions.yml